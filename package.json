{
  "name": "cronograma-historia-tests",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:encoding": "python3 scripts/check_encoding.py"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0"
  }
}
